# ────────────────────────────────────────────────────
# Yuno Agent Platform — Python dependencies
# Generated from actual imports in app/
# ────────────────────────────────────────────────────

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Database (async SQLite — no Postgres needed)
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.2.0

# LLM runtime
langchain-core>=0.2.0
langchain-groq>=0.1.6
langgraph>=0.1.14

# Telegram bot
python-telegram-bot>=21.0

# Environment / config
python-dotenv>=1.0.0

# Test utilities (not installed in Docker image — dev only)
# pytest>=8.0
# pytest-asyncio>=0.23
# httpx>=0.27  # only needed for TestClient in tests
